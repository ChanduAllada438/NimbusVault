# Copy this file to `.env` and fill with your real values. Do NOT commit `.env`.
# Example values below — replace with your own MongoDB connection string and secrets.

MONGO_URI=mongodb://username:password@host:port/databasename
PORT=5000
JWT_SECRET=your_jwt_secret_here
